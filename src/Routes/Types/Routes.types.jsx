/**
 * This file help us to get the path for all the routes
 * for the different views and avoid typos.
 *
 * the name of the type always start with the word path and the name of the route.
 * the path always start with / and never finish with /
 */
export const pathSearch = "/search";
